# T-019 · Consolidated multi-name performance report (Docs-Lane / Reporting Epic)

**Epic:** Reporting & Portfolio Analytics · **Owner:** tech-writer → sr-dev review · **Status:** To Do

## Goal
Turn the batch runner's per-name results into ONE consolidated document — every pilot name side by
side: per-name table, equity curve, honest HOLD verdicts — instead of scattered single-symbol files.
This is the "performance reports on our strategy vs buy&hold" artifact @user asked for.

## Depends on
T-016 Done (batch runner emits stable machine-readable per-name results + per-name MD reports). Do
NOT build this against today's `backtest_results.json` — sr-dev flagged a shipped-key bug there
(`momentum_12_1` currently holds regime-aware's number, not momentum's); T-016 fixes the key.

## Deliverables
1. One consolidated report (e.g. `reporting/CONSOLIDATED-PERFORMANCE.md`) — per-name table
   (net return, CAGR, Sharpe, Sortino, max DD, exposure, edge vs buy&hold, verdict) for all pilot
   names in one view; beat count across the universe.
2. Per-name equity curves + honest verdicts side by side.
3. A short narrative: which regimes the strategy is capturing (or not) across the 5 names — this is
   regime-diagnostic even when every verdict is HOLD.

## Acceptance / sr-dev checks
- Assembled from T-016's stable output, not hand-typed numbers.
- Every number traces to a per-name report / JSON; no lookahead claims.
- Honest: verdicts shown as-is (incl. HOLD/negative edge), no polishing over the known trend-only
  filter limitation — footnote that the macro leg is unimplemented and numbers are trend-only.

## Notes
- This is assembly/rendering (tech-writer's lane); the engine loop + metrics stay coder/sr-dev (T-016).
- Re-runnable: once T-016 ships, regenerating this doc against a future signal (macro leg) is cheap —
  don't over-polish against the current trend-only filter.
